import React from "react";

const Notes = () => (
  <section className="notes">
    <nav>
      <ul className="nav-links">
        <li>create</li>
        <li>delete</li>
      </ul>
    </nav>
  </section>
);

export default Notes;
